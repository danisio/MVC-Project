@using Kendo.Mvc.UI;
@functions{
    private const string ControllerName = "Questions";
}
@{
    ViewBag.Title = "Questions";
}

<div class="row">
    <div class="text-center">
        <h2>Questions</h2>
    </div>

    <div>
        @(Html.Kendo()
    .Grid<MySurveys.Web.Areas.Administration.ViewModels.QuestionViewModel>()
    .Name("grid-questions")
    .Columns(columns =>
    {
        columns.Bound(model => model.Id);
        columns.Bound(model => model.Content);
        columns.Bound(model => model.SurveyTitle);
<<<<<<< HEAD
        columns.Bound(model => model.ParentContent);
=======
        columns.Bound(model => model.ParentId);
>>>>>>> 3ba4d80a94aa6ddc838c100443d476ce574c1299
        columns.Bound(model => model.IsDependsOn);
        columns.Bound(model => model.TotalPossibleAnswers);
        columns.Bound(model => model.CreatedOn).Format("{0:dd/MM/yyyy HH:mm}");
        columns.Bound(model => model.ModifiedOn).Format("{0:dd/MM/yyyy HH:mm}");
        columns.Command(command =>
        {
            command.Edit();
            command.Destroy();
        }).Title("Options");
    })
    .Pageable(page =>
    {
        page.Refresh(true);
        page.PreviousNext(true);
        page.Numeric(true);
    })
    .Sortable(sort =>
    {
        sort.AllowUnsort(false);
        sort.SortMode(GridSortMode.SingleColumn);
    })
    .Editable(edit =>
    {
        edit.Mode(GridEditMode.PopUp);
    })
    .Filterable()
    .Resizable(resize => resize.Columns(true))
    .DataSource(data =>
    {
        data.Ajax()
            .Model(m =>
            {
                m.Id(b => b.Id);
                m.Field(b => b.TotalPossibleAnswers).Editable(false);
<<<<<<< HEAD
                m.Field(b => b.ParentContent).Editable(false);
=======
                m.Field(b => b.ParentId).Editable(false);
>>>>>>> 3ba4d80a94aa6ddc838c100443d476ce574c1299
            })
            .PageSize(5)
            .ServerOperation(true)
            .Events(ev => ev.Error("errorAlert"))
            .Read("Read", ControllerName)
            .Update("Update", ControllerName)
            .Destroy("Destroy", ControllerName)
            .Sort(sort => sort.Add("Id").Ascending());
    }))
    </div>
</div>

<script>
    function errorAlert(err) {
        //// console.log(err);
        alert("An error occurred");
    }
</script>